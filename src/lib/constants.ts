/**
 * App-wide constants.
 * Import from here instead of hardcoding values across the codebase.
 */

/** The category ID used as the proxy for the global variable spending budget. */
export const VARIABLE_CATEGORY_ID = 'cat_groceries';

/** Default locale used for Intl formatters throughout the app. */
export const DEFAULT_LOCALE = 'en-US';

/** Default currency used for Intl.NumberFormat and display. */
export const DEFAULT_CURRENCY = 'USD';
