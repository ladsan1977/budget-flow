// This component is intentionally empty.
// The onboarding overlay has been replaced by the inline DashboardEmptyState component,
// which is rendered directly inside DashboardDesktop and DashboardMobile when a month
// has no transactions — following the Single Responsibility Principle.
export { };
